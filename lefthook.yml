pre-commit:
  parallel: false
  commands:
    format:
      glob: '*.{js,ts,vue,json,css,md}'
      run: npx prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: '*.{js,ts,vue}'
      run: npx eslint --fix {staged_files}
      stage_fixed: true
